<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" Title="BITTR" x:Class="Bittr.Views.HomePage">

    <ContentPage.Content>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>


            <StackLayout Grid.Row="0" Orientation="Horizontal" HorizontalOptions="StartAndExpand" Padding="16">

                <StackLayout Orientation="Vertical">
                    <Label Margin="0, 0, 32, 0" FontFamily="{StaticResource RegularFont}" FontSize="24" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="End" Text="What are you " />

                    <Label Margin="0, 0, 32, 0" FontFamily="{StaticResource BoldFont}" FontSize="66" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="End" Text="BITTER" />

                    <Label Margin="0, 0, 32, 0" FontFamily="{StaticResource RegularFont}" FontSize="24" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="End" Text="about today?" />

                </StackLayout>

                <Image Source="lemon1" WidthRequest="100" HorizontalOptions="Start" />

            </StackLayout>

            <StackLayout Grid.Row="1" Orientation="Horizontal" Margin="16">
                <Entry Text="{Binding NewComplaint.Text}" Placeholder="I'm bitter about..." HeightRequest="50" HorizontalOptions="FillAndExpand"/>
                <ImageButton BorderColor="{StaticResource LightYellow}" BackgroundColor="{StaticResource MediumYellow}" Source="return.png"  WidthRequest="50" HeightRequest="50" Padding="10" HorizontalOptions="End" Command="{Binding AddComplaintCommand}"/>
            </StackLayout>


            <ListView Grid.Row="2" ItemsSource="{Binding Complaints}" HasUnevenRows="True" Margin="16,32,16,0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Grid.Row="2" Orientation="Vertical">

                                <Label Text="{Binding Text}" FontSize="Large" />
                                <Label Text="{Binding Creator.Username}" FontSize="Medium" />

                                <Label Text="{Binding Timestamp}" FontSize="Caption" />

                                <Image Source="{Binding ImageName}" HorizontalOptions="FillAndExpand" Aspect="AspectFit" />

                                <ImageButton Source="up.png" WidthRequest="50" Aspect="AspectFit" />
                                <ImageButton Source="down.png" WidthRequest="50" Aspect="AspectFit" />


                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>



        </Grid>

    </ContentPage.Content>
</ContentPage>
